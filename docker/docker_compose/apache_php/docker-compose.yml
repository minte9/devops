version: "3.9"

services:
  web:
    build:
      context: ./app
    container_name: my-apache-php
    ports:
      - "8081:80"
    volumes:
      - ./app/src:/var/www/html
    restart: unless-stopped